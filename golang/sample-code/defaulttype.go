package main

type gram float64
type ounce float64

func main() {
	// integer literal default type is int not int32 or int64 or int 16
	// as later if we need we can convert the default type
	// float default type is float64
	// bool default type is bool
	// string default type is string
	// character default type is rune

	// var g gram = 1000
	// var o ounce
	// o = ounce(g) * 0.035274
	// fmt.Printf("%g grams is %.2f ounces\n", g, o)

	type MyDuration Duration

}
